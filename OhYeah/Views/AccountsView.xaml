<controls:BaseView x:Class="OhYeah.Views.AccountsView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:controls="using:OhYeah.Controls"
                   mc:Ignorable="d"
                   Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                   DataContext="{Binding Accounts, Source={StaticResource Locator}}">

    <Grid Background="{StaticResource BackgroundBrush}"
          Margin="0,32,0,0">
        <ListView ItemsSource="{Binding Providers}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:AccountControl Height="60"
                                             Margin="19,0,19,12"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</controls:BaseView>
